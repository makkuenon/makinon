import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(spacing: 16) {
            Text("Makinon iOS Signer")
                .font(.title)
                .bold()

            Text("UIKit + SwiftUI hybrid")
                .font(.subheadline)
                .foregroundColor(.secondary)
        }
        .padding()
    }
}
